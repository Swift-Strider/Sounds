{
    "$schema": "https://getcomposer.org/schema.json",

    "name": "diamondstrider1/sounds",
    "version": "1.2.0",
    "type": "library",
    "license": "MIT",

    "require": {
        "pocketmine/pocketmine-mp": "^4.7"
    },

    "scripts": {
        "generate_sounds": [
            "curl https://aka.ms/resourcepacktemplate -L > pack.zip",
            "php tools/generate_sounds.php"
        ]
    }
}
